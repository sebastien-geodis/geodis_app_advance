<%= form_for(@intervention_status) do |f| %>
  <% if @intervention_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intervention_status.errors.count, "error") %> prohibited this intervention_status from being saved:</h2>

      <ul>
      <% @intervention_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
